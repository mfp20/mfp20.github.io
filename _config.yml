# Site settings
title: mfp20 - minority-reported only
url: "http://mfp20.github.io"

# Build settings
# markdown: redcarpet
markdown: kramdown
redcarpet:
  extensions: ["tables", "autolink", "strikethrough", "fenced_code_blocks"]
